# Historical tracked expanded/resolved spec payloads. The gate is shrink-only;
# every new detected path fails until deliberately enumerated with an owner and
# rationale. The historical stock has now been migrated, so the list is empty.
schema_version = 1
violations = []
