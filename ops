{"author":{"id":"4bb6c1767e50694cfde61856db22bf137d1651e8a0b5ca858c486db3dd02f280"},"ops":[{"type":1,"timestamp":1778536831,"nonce":"OQVnLE2sqSed+2jz4cZ5k4rkyWg=","title":"Lit-replication 6-cell matrix: corrected smoothness × jerk × position shape","message":"Tests the effects of (a) correcting `nn_hidden_derivative` to Shahbazi-effective `1e-3` (was `1e2` in prior 6-cell `2bc95fd`, off by 1e5×), (b) endpoint jerk on/off (`nn_output_jerk ∈ {1e5, 0}`), (c) position-cost shape (flat / `(t/N)^6` post-go / `(t/N)^6` throughout).\n\n## Matrix\n\nAll cells share:\n- GRU, `n_replicates=5`, `n_warmup_batches=12000`, `batch_size=250`, `seed=42`\n- `effector_hold_pos = effector_pos_running = 10.0` (equal-weight = Shahbazi's uniform-time target-switching mechanism)\n- `effector_pos_late = 0`, `effector_vel_late = 0`, `effector_hold_vel = 0`, `effector_final_vel = 0` (no rlrmp-specific anti-anticipation augmentations)\n- `nn_output = 1e-5`, `nn_hidden = 1e-5`, `nn_hidden_derivative = 1e-3` (Shahbazi-effective)\n- `nn_output_pre_go = 0`, `nn_hidden_derivative_pre_go = 0` (no pre-go masks)\n- `p_catch_trial = 0.5` (matches Shahbazi 2025 §4.2)\n- `loss_update_enabled = False` (explicit weights for cross-cell interpretability)\n- Adversary: `linear_dynamics` (matches recent rlrmp convention)\n\n| Cell | `nn_output_jerk` | Position shape |\n|---|---|---|\n| 1 | 1e5 | flat |\n| 2 | 1e5 | (t/N)^6 post-go, flat pre-go |\n| 3 | 1e5 | (t/N)^6 throughout (≈ C\u0026S 2019) |\n| 4 | 0 | flat |\n| 5 | 0 | (t/N)^6 post-go, flat pre-go |\n| 6 | 0 | (t/N)^6 throughout |\n\n## Prerequisites\n\n- **Power-law schedule support** in `make_late_discount_from_epoch` (issue `2e1a6ad`) — must land before cells 2, 3, 5, 6 can run faithfully.\n\n## Decision criteria\n\n- **Primary**: vel-RMSE-of-velocity-profile ratio (within-cell / nearest-across-cell), threshold \u003c 0.5.\n- **Secondary**: hold drift (mm), peak forward velocity (m/s), time-to-peak.\n- **Reporting**: both CV (peak velocity scalar) and vel-RMSE ratio; position-RMSE ratio for cross-check.\n\n## Conditional follow-up\n\n- **If jerk-disabled cells (4-6) show significant anticipation** vs jerk-enabled cells (1-3) → introduce `nn_output_pre_go` lever in a follow-up matrix (try weights 1e-2, 1e-1, 1.0).\n- **If 6-cell results suggest position-vs-control miscalibration** (e.g., peak velocity far from biological range, anomalous reach kinematics) → run position-weight + target_ratio sweeps (separate issue).\n\n## References\n\n- Prior `2bc95fd`: original anti-anticipation 6-cell (with the now-known smoothness weight error).\n- `2e1a6ad`: power-law schedule (prerequisite).\n- `cea2e62`: analytical bridge for rlrmp cost (parallel analysis on no-jerk variants).\n- `c99ad9d`: training-methods coord (will receive cross-ref comment).\n- `4d38c15`: analyses coord (variance metric).\n- `_artifacts/scratchpad/literature_replication_proposal.md`: design background.","files":null}]}