{"author":{"id":"4bb6c1767e50694cfde61856db22bf137d1651e8a0b5ca858c486db3dd02f280"},"ops":[{"type":1,"timestamp":1778347162,"nonce":"uWyMRQvdLLqJJ8t5LhvUuRVxfyY=","title":"train_minimax.py: write run.json to output-dir on completion","message":"The training script `scripts/train_minimax.py` does not currently write a `run.json` to its output directory. Per CLAUDE.md \"Adding a new experiment\" + post-training-run protocol, every run should have a `run.json` capturing the CLI hyperparameters that produced the model weights — stable, one per run, reproducibility-load-bearing.\n\nCurrently, the spec has to be reconstructed manually from RUN_PLAN.md flags + ps capture (done for the 6-cell matrix in commit `b5e6e6c` post-hoc). This is a workflow gap that bites every multi-cell run.\n\n**Implementation:** at the end of `train_minimax.py` (or wherever final outputs are written), serialise the parsed argparse Namespace + git SHA + a few environment fields (gpu type, datacenter if present) to `\u003coutput_dir\u003e/run.json`. Lightweight; ~20 lines.\n\nSurfaced from `2bc95fd` (anti-anticipation 6-cell matrix).","files":null}]}