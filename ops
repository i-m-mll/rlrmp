{"author":{"id":"4bb6c1767e50694cfde61856db22bf137d1651e8a0b5ca858c486db3dd02f280"},"ops":[{"type":3,"timestamp":1778218612,"nonce":"XXz/PP6LEihEl/dxSxsSBI3gjhc=","message":"Filed `76d3a8e` (rlrmp) consolidating RunPod operational lessons surfaced from the flavor-B training sweep (issue `c723082`). Cross-cutting subset that generalises beyond rlrmp:\n\n- **zsh-vs-bash word-splitting** (`§C2` of `76d3a8e`) — reproducible: `SSH=\"...\"; $SSH 'cmd'` works in bash but fails in zsh (treats expansion as one literal command name). Affects any monitoring loop in zsh; relevant to dotfiles, mandible, and any cross-project tooling that wraps SSH/curl in a poll loop.\n- **`uv sync` overwrites CUDA jaxlib post-`uv pip install jax[cuda12]`** (`§A1`) — affects every JAX-on-cloud-pod workflow, not just rlrmp. Could surface in feedbax cloud experiments.\n- **`uv run` triggers implicit sync, races N-process** (`§A2`) — affects any parallel-process JAX workflow.\n- **Schema discovery for deferred tools** (`§F2`) — workflow lesson for Claude Code agents broadly, not rlrmp-specific. Iterating 5+ calls each surfacing one missing-field error wastes turns.\n\nThe first three may warrant CLAUDE.md additions (per **Memory Files vs Workflow Instructions** — file dotfiles issues if you want them captured globally). The fourth is more of an agent self-discipline note.\n\nNo action requested here; this is a heads-up. The substantive content is in `76d3a8e`.","files":null}]}