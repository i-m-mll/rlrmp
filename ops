{"author":{"id":"4bb6c1767e50694cfde61856db22bf137d1651e8a0b5ca858c486db3dd02f280"},"ops":[{"type":1,"timestamp":1778515962,"nonce":"1eZpJD/RpP2e5CFwWuEMtzJHROI=","title":"Strip 1_general.assets/ and 2_general.assets/ PNG dumps from git history","message":"The `results/1_general.assets/` and `results/2_general.assets/` directories contain ~92 MB of committed PNGs (552+ files) — notebook-embedded figures, many above the 300 KB thumbnail limit. They predate the current `_artifacts/` vs `results/` convention; under that convention they would all live in `_artifacts/`.\n\n**Plan**:\n1. Treat both as artifacts (move to `_artifacts/` semantically; track only thumbnails ≤300 KB and spec.json siblings in `results/`).\n2. Strip from git history via `git filter-repo` to recover repo size. Without this, every clone carries the 92 MB.\n\n**Not immediate.** Filter-repo requires a fresh clone of the repo afterwards (every existing clone becomes incompatible with the rewritten history) and must happen during a quiet moment — no other in-flight work on the repo. Coordinate before doing it.\n\n**Out of scope for first pass**: deciding what happens to the .md notebook files those PNGs are embedded in. Likely they keep their PNG references, but those resolve to symlinks into `_artifacts/` after the dual-tree migration.\n\nSurfaced from results/ structure review (`f485c26`).","files":null}]}