{"author":{"id":"4bb6c1767e50694cfde61856db22bf137d1651e8a0b5ca858c486db3dd02f280"},"ops":[{"type":1,"timestamp":1778188227,"nonce":"N2BX0ExS176nc3snUc1jV9fl0WY=","title":"First induced-gain run across Part 2.5 checkpoints (baseline / vanilla / minimax / mult / ratio03)","message":"**Goal.** Run the induced-gain analyser (`74bfd86`/`aa60d3f`) on existing Part 2.5 trained checkpoints, produce the first cross-method table of `||T_{w→z}||` values. Tests the analyser against real `feedbax_graph_controller` integration (the audit fix `b131510` was synthetic-Graph only) and provides the first empirical input to the flavor (a) ⊊ (b) discrimination on actual networks.\n\n**Checkpoint groups under `_artifacts/part2_5/runpod/`:**\n- `baseline/` (standard backprop)\n- `vanilla_single/`, `vanilla_pop5/` (vanilla training, single seed vs pop=5)\n- `minimax_single/` (seeds 0–2, adversarial; flavor (a) parametric force-field adversary)\n- `mult_single/`, `mult_pop5/` (multiplicative noise)\n- `ratio03_single/`, `ratio03_pop5/` (ratio=0.3 sweep)\n- `tier1_redo/`, `bench/`, `ratio_sweep/` (parameter sweeps; deprioritise)\n\n**Headline channels** (per analyser API): `additive_force × qr_cost` for the Riccati-comparable scalar, `structural_da × qr_cost` for the (a) ⊊ (b) test, `sensory_perturbation × qr_cost` for the analogue of the existing feedback-perturbation analysis. `peak_velocity` z-channel for behavioural sanity.\n\n**Expected output:**\n- `results/part2_5/induced_gain_first_run/run.json` — analysis spec (channels, plant, schedule, horizon, analyser version).\n- `results/part2_5/induced_gain_first_run/notes.md` — short narrative covering the table, the API integration friction (or lack thereof), and any gotchas surfaced.\n- `_artifacts/part2_5/induced_gain_first_run/gains.npz` — per-(checkpoint, w_channel, z_channel) gain values.\n- `scripts/run_induced_gain_part2_5.py` — the runner.\n\n**Out of scope:** plotting/figures (separate spec issue once we know what's worth plotting); per-checkpoint timesteps (just final/converged checkpoints first); deep dive into specific anomalies (those become their own follow-up issues).\n\n**Cross-refs.** `74bfd86` (analyser); `b131510` (feedbax adapter); methodology-fix phase `b557d4e` (closed); analyses coord `4d38c15` (will get the tier-shift comment with actual numbers).","files":null}]}