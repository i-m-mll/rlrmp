{"author":{"id":"4bb6c1767e50694cfde61856db22bf137d1651e8a0b5ca858c486db3dd02f280"},"ops":[{"type":3,"timestamp":1778555900,"nonce":"Ygu+5Yn1Nz6xUmdiw+cS3OZoXIM=","message":"**MVP result (2026-05-11)** — informative null. Commit `20ae797` on `feature/linear-controller-mvp`, auth `007b8e0f`.\n\n## Headline\n\nΔv under `LinearDynamics` disturbance:\n\n| Architecture | Δv@0.5 | Δv@1.0 | Δv@1.5 |\n|---|---|---|---|\n| `linear_regulator` | +0.010 ± 0.009 | +0.035 ± 0.015 | +0.060 ± 0.019 |\n| `linear_tracker` | +0.011 ± 0.009 | +0.036 ± 0.015 | +0.061 ± 0.019 |\n| GRU `lit__post_nojerk` | +0.002 ± 0.001 | +0.004 ± 0.002 | +0.005 ± 0.002 |\n\n## Discriminator outcome\n\n**Did not split cleanly.** Regulator gives Δv \u003e 0 (predicted ✓), but tracker gives statistically identical Δv \u003e 0 (predicted ≈ 0, observed regulator-collapsed). Both linear controllers give larger Δv than the GRU baseline by ~6-12×.\n\n## Mechanism\n\n`|u_ff|_∞ ≈ 0.02` — essentially zero. The optimizer never used the feedforward channel. The tracker is *capable* of decoupling (strictly larger parameter space) but at 1000 batches of warmup-only training on laptop CPU, the optimizer collapsed it onto the regulator solution. The MVP rules out neither 'decoupling-via-parameterisation is the mechanism' nor 'decoupling-via-parameterisation is hard to learn'.\n\n## Implementation notes\n\nZero feedbax changes. New `Component` subclasses live entirely in rlrmp. Step counter stored in `state.hidden` rather than adding a 'time' task input (avoids SISU input ordering issue). Both 1000-batch runs ~50s wall time on CPU. Code:\n- `src/rlrmp/networks/linear_controllers.py` (430 lines, new)\n- `src/rlrmp/models.py` + `src/rlrmp/modules/training/part2.py` dispatch additions\n- `scripts/train_minimax.py` `_get_trainable` / `_make_where_train` branch on `type(net).__name__`\n- `scripts/analyse_linear_decoupling_mvp.py` (~330 lines, new)\n\n## Follow-ups (documented in `results/410d7ac/notes/decoupling_acid_test_mvp.md`)\n\n- Longer training budget (10k-50k batches) to give the optimizer a chance to discover `u_ff`\n- Initialize `u_ff` near the analytical feedforward (warm-start) rather than zero\n- Non-trivial `x_nom(t)` (currently the regulator's nominal trajectory under zero disturbance, which gives `u_ff = -K·x_nom` collapsing to regulator form)\n- Train with adversarial phase (n_adversary_batches \u003e 0) — the disturbance might break the regulator/tracker equivalence the optimizer is exploiting\n- Full 4-architecture sweep from parent `d448c9d` (affine recurrent + GRU) once a discriminating training regime is identified","files":null}]}