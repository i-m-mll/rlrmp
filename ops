{"author":{"id":"4bb6c1767e50694cfde61856db22bf137d1651e8a0b5ca858c486db3dd02f280"},"ops":[{"type":1,"timestamp":1778109101,"nonce":"jk3jB3Nk8uUdtLA1Aulu+OTx2AE=","title":"Project meta coordination: cross-project / workflow concerns","message":"Coordination surface for **cross-project / workflow concerns** surfaced during rlrmp work. Per umbrella-verbosity directive (`1ba096f`), this body is minimal — comments index the cross-cutting concerns; the actual fixes live in the destination repos.\n\n## What goes here\n\nWhen working on rlrmp, you may surface concerns affecting:\n- **Mandible** (auth flow, issue tracking, hooks)\n- **Feedbax** (downstream library)\n- **Dotfiles** (global CLAUDE.md, scripts in `~/.dotfiles/bin/`)\n- **General workflow** / tooling\n\nFile the actual issue in the destination repo. Then comment HERE with: destination repo + issue ID, one-line rationale, optional link back to the rlrmp work that surfaced it.\n\n## What doesn't go here\n\nProject-internal concerns → respective coord (`4d38c15`, `c99ad9d`, `b33e8da`). Bugs/features entirely within rlrmp → standard issue.\n\n## Cross-refs\n\n- `4d38c15` — analyses\n- `c99ad9d` — training-methods\n- `b33e8da` — phases\n- `4dabb63` — future axes (manuscript, model-structure)\n\n## Initial seed\n\n`1ba096f` (this repo) — Add CLAUDE.md directive: higher-level umbrellas/coordination minimal + cross-ref. The meta concern that prompted this issue. Filed in rlrmp because it's a project-CLAUDE.md change; could later be promoted to global CLAUDE.md (dotfiles).\n\nLabel: `coordination`. Do NOT reference in commit `Bug:` trailers (auto-close skip enforced via mandible commit but caution remains).","files":null}]}