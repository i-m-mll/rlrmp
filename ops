{"author":{"id":"4bb6c1767e50694cfde61856db22bf137d1651e8a0b5ca858c486db3dd02f280"},"ops":[{"type":3,"timestamp":1778273224,"nonce":"9caZKp5vHPqAflU60g2iQE9/I+Y=","message":"**Hypothesis-disposition reframe — re: 9a0558e structural lift after recipe-bug audit.**\n\nThe audit (`/tmp/flavor_ab_review/findings/cs_alignment_audit.md`) reframes the dispositions of G1, G2, G5, G6 from the prior comment:\n\n- **G1 (delay augmentation, 5-step lag).** Mechanically correct in isolation but was rendered inert by the Q zero-padding bug in `_apply_delay_augmentation` and by the obs-channel-only lag (n_obs=4 vs MATLAB's n=8 full-state). With the recipe fixes (full-state lag + distributed Q per `AugRobustControl.m`, commits `215a06a` on `feature/cs-faithful-8state-delay`) it contributes as expected.\n\n- **G6 (8-state plant + integrator coupling).** Same story: structurally correct, but the integrator entries 6,7 of Q were zero-padded instead of constant-1 (`script_minmax_pointMass.m` line 30). Note: under `B_w = I_8`, integrator columns are *adversary-redundant* — a real property of C\u0026S's H∞ game, not a bug; documented in `cs_faithful_pointmass` docstring.\n\n- **G5 (horizon mismatch, n_steps 80 vs 60).** Was deprioritized; the audit decomposition shows it is **the largest single factor** (~−3.5% of the gap). **Promote from 'deprioritized' to 'key parameter for C\u0026S replication.'** The canonical post-fix probes use `n_steps=60` (= 0.6 s reach at dt=0.01) matching C\u0026S's `simdata.nStep = 61` minus the terminal stage offset.\n\n- **G2 (Kalman estimator).** Mechanically correct as a description of C\u0026S's full simulation pipeline (sensory + motor noise, output-feedback through delayed measurement) but **not load-bearing for the analytical Fig 1e replication.** The deterministic Riccati pipeline alone reproduces +Δv after recipe fixes. Issue `83fc5b5` updated to reflect demotion (still open as future investigation).\n\nAfter fixes: canonical 15 cm Δv = +8.31% at γ → γ\\*, matching MATLAB port (+8.24%) and Fig 1e (~+7.76%).\n\nCross-refs: audit `/tmp/flavor_ab_review/findings/cs_alignment_audit.md`; fix branch `feature/cs-faithful-8state-delay` (commits `215a06a`, `02cfdf7`, `e1cd78e`); synthesis correction on `feature/synthesis-cs-target-reframe` (commit `5555e06`).","files":null}]}