{"author":{"id":"4bb6c1767e50694cfde61856db22bf137d1651e8a0b5ca858c486db3dd02f280"},"ops":[{"type":3,"timestamp":1778557429,"nonce":"sCeEozG9TD0nm6FMRAhMGyWz23w=","message":"**Corrected MVP result (2026-05-11)** — discriminator did NOT split. Commits `45cc2db` + `dfac0d0` on `feature/linear-controller-mvp`, auth `4696b654`.\n\n## Four-model design\n\n| Run | Phase | Final loss | peak_v @ pert=0 (m/s) |\n|---|---|---|---|\n| `linear_regulator__baseline` | warmup only | val 2.56 | 0.158 ± 0.006 |\n| `linear_regulator__adversarial` | warmup + adv | ctrl 1.29 | **0.280 ± 0.006** |\n| `linear_tracker__baseline` | warmup only | val 2.55 | 0.158 ± 0.005 |\n| `linear_tracker__adversarial` | warmup + adv | ctrl 1.30 | **0.280 ± 0.007** |\n\nSeed=42 across all runs (same warmup state between baseline/adversarial pairs). LinearDynamicsAdversary (ΔA·x), 5 PGD inner steps, eta_max=0.1. Adversary ΔA Frobenius norm saturated 0.1 ceiling for both adversarial runs.\n\n## Headline Δv\n\n```\nΔv_regulator = +0.775 ± 0.034 (SEM, n=5)\nΔv_tracker   = +0.776 ± 0.034 (SEM, n=5)\n```\n\n**Statistically identical.** Both architectures show +77.5% velocity inflation under adversarial training. The d448c9d discriminator prediction (regulator → +Δv, tracker → ≈0) is **NOT supported**.\n\n## u_ff utilization in the tracker\n\n| | |u_ff|_max | |u_ff|_L2 mean |\n|---|---|---|\n| baseline | 0.021 | 0.010 |\n| adversarial | 0.052 | 0.040 |\n\nAdversarial training pushed u_ff ~4× over baseline — so the optimizer DID start to engage the feedforward channel — but still vastly smaller than the K-driven feedback (typical `|K·e|` ~ 0.5 with K_max ≈ 4.8). The optimizer prefers a K-heavy solution even when u_ff is available.\n\n## Implications\n\n- **Regulator-inflation claim**: strongly supported. Linear controllers under H∞-style adversarial training inflate peak velocity by ~78%.\n- **Tracker decoupling claim**: not supported at this MVP scale. Adding a feedforward channel does not prevent velocity inflation. Either (a) decoupling-via-parameterisation is not the mechanism, (b) 500 adversarial batches at MVP scale is still insufficient for the optimizer to find the decoupled solution, or (c) the specific tracker design (free `u_ff` parameter, no `x_nom` injection) does not capture whatever decoupling mechanism the GRU uses.\n\n## Resumability\n\n`train_minimax.py`'s existing `--resume` flag supports full mid-training resumption. `warmup_model.eqx` + `checkpoints_adversarial/checkpoint_latest/` contain model + adversaries + ctrl \u0026 adv optimizer states + batch_idx + loss histories. RNG caveat (`key_adv` re-derived from seed, deterministic but not bitwise-identical to an uninterrupted run) documented per-run in each `run.json`. Baselines can only be 'extended' (re-trained with larger `--n-warmup-batches`), not strictly resumed mid-warmup.\n\n## Bug fix included\n\n`train_minimax.py` raised `AttributeError: 'LinearController' object has no attribute 'hidden'` on the first adversarial batch — three `eqx.tree_at` sites used `lambda m: (m.nodes['net'].hidden, m.nodes['net'].readout)` unconditionally. Extracted a `_trainable_where(model)` helper mirroring `_get_trainable`'s class-branching. The prior MVP hadn't hit this because `n_adversary_batches=0` skipped the loop body.\n\n## Follow-up directions (not yet filed)\n\n1. **Longer adversarial phase**: 500 → 2000 or 5000 batches to give the optimizer more time to discover a decoupled solution\n2. **Non-trivial x_nom**: currently the tracker's nominal trajectory makes `u_ff = -K·x_nom` collapse to regulator-equivalent; injecting a separately-computed reference trajectory would break this\n3. **Warm-start u_ff**: initialize `u_ff(t)` near the analytical feedforward (computed from the regulator solution) instead of zero\n4. **Architecture variant**: try u_ff parameterised as a function of reach direction (rather than just time) — currently u_ff has no input-dependent expressivity\n5. **Affine recurrent + GRU baselines** from `d448c9d` for the full 4-architecture sweep","files":null}]}