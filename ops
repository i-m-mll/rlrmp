{"author":{"id":"4bb6c1767e50694cfde61856db22bf137d1651e8a0b5ca858c486db3dd02f280"},"ops":[{"type":3,"timestamp":1778224617,"nonce":"w2+sgTbs4hbAZjDX3TKHMDXNX0w=","message":"**Attempt tracking — quadratic-stability / S-procedure approach.**\n\nApproach selected: extend production Riccati with an additive entry point (`solve_hinf_riccati_modelclass`) that accepts a Frobenius-ball budget `eta` and a state-selector `C_q` (rows for [pos, vel] of the augmented state). The flavor-(b) ΔA perturbation `f = mass · ΔA · [pos,vel]` (matching feedbax `DynamicsMatrixPerturb`, develop branch) is folded into the Riccati synthesis via the S-procedure / quadratic-stability reduction:\n\n- Treat `w_t = mass · ΔA · C_q · x_t` as an unstructured fictitious disturbance entering through the standard force channel `B_w`.\n- ‖ΔA‖_F ≤ η implies ‖w_t‖_2 ≤ mass·η·‖C_q·x_t‖_2 (Cauchy-Schwarz on Frobenius-induced operator norm).\n- Sufficient condition for robust stability with LQ performance: solve the standard H∞ Riccati on B_w but with the performance-output Q augmented by `(mass·η)² · C_q^T · C_q`.\n- γ\\*^(b)(η) = smallest admissible γ for the augmented Riccati. Monotone in η; recovers γ\\*^(a) at η=0.\n\nFlavor-(a) interface preserved: `solve_hinf_riccati` unchanged, modelclass solve is a new wrapper that augments Q in-place and dispatches.\n\nWhy quadratic-stability over state-coupled time-varying B_w(x_t):\n- LinearDynamicsAdversary holds ΔA constant across the trial — exactly the parametric-uncertainty class quadratic stability handles.\n- Empirical γ_sd from analyser uses `structural_da` channel (small-gain framing) — same conceptual class.\n- LTI primitive — directly comparable to existing flavor-(a) γ\\*; trivial to bisect.\n- State-coupled time-varying B_w(x_t) requires nominal-trajectory finite-horizon DRE; deferred per task constraints.\n\nExpected behaviour (testing in progress):\n- γ\\*^(b)(η) \u003e γ\\*^(a) for η \u003e 0 (more conservative attenuation needed against structured perturbation).\n- xfailed `test_cs_faithful_qr_velocity_inflation` should turn positive Δv at suitable η on C\u0026S plant (flavor-B exploits the position channel, which gets weight 1e6 in Eq. 15 Q vs ~zero for vel cost coupling in flavor-a).\n- Conservatism: this gives an upper bound on γ\\*^(b); the true achievable robust-controller γ may be lower with full structured-singular-value (μ-synthesis) treatment. Documenting as future work.\n\nCross-ref: feedbax `DynamicsMatrixPerturb` (force-channel embedding) confirmed at develop branch. Test pivotal: re-run xfailed `test_cs_faithful_qr_velocity_inflation` under flavor-(b) and report whether Δv \u003e 0.","files":null}]}