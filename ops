{"author":{"id":"4bb6c1767e50694cfde61856db22bf137d1651e8a0b5ca858c486db3dd02f280"},"ops":[{"type":3,"timestamp":1778714501,"nonce":"UeIxoYglluDQwsy0Me3/psq0Pw8=","message":"**Clarification on feedbax integration — must be researched, not assumed.**\n\nThe original body referenced \"feedbax's existing declarative analysis system\" but I did not actually review feedbax to confirm what that system looks like, what its scope is, or whether it fits the rlrmp matrix-analysis shape. That phrasing was speculative.\n\n**Mandatory first step for whoever picks this up**: read feedbax at `~/Main/10 Projects/10 PhD/20 Feedbax/feedbax/worktrees/develop/` and inventory its analysis / plotting / declarative-config primitives. Likely places to look:\n- `feedbax/plot/` — any grid/subplot/save_figure helpers we should route through\n- `feedbax/analysis/` if it exists — declarative analysis configs\n- `feedbax/task.py` and `feedbax/train.py` — any existing experiment-spec / config-driven entry points\n- Any tests that exercise a declarative analysis flow (often the cleanest examples)\n\n**Preference order for the rlrmp declarative pipeline**:\n1. **Use feedbax primitives directly** wherever they fit. If feedbax already has a 'one config -\u003e N figures + metrics tables' abstraction, build rlrmp's pipeline on top of that abstraction rather than reinventing.\n2. **Extend feedbax** if a primitive is close-but-not-quite. Per rlrmp CLAUDE.md (Feedbax Integration section): plant- or task-general abstractions belong in feedbax, not rlrmp. So if the declarative pipeline needs a generic primitive that feedbax doesn't yet expose, the right move is to land it in feedbax first (separate auth request, develop branch), then build the rlrmp pipeline on top.\n3. **Only build rlrmp-side** for the rlrmp-specific concerns: cell-list declaration, project-specific metrics (within-cell vel-RMSE etc.), and the experiment-folder layout integration.\n\nThis ordering avoids reinventing feedbax functionality in rlrmp, which would create maintenance burden and drift from feedbax's APIs over time.","files":null}]}