Issue 1ab1fef locks a no-launch follow-up row under the adaptive soft-adversary
method line. The row keeps the 91a090c short schedule and baseline contract, but
uses epsilon-scaled outer training: the direct-epsilon inner maximizer selects a
full-strength cap-free soft-energy adversary, then the controller update applies
the outer schedule as a physical epsilon-channel scale instead of blending clean
and adversarial controller losses.
