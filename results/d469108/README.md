Issue `d469108` reruns the frozen Adam soft-lambda reliability audit against the
corrected cap-independent HVP/p90 lambda source and corrected direct-epsilon /
closed-loop reference artifacts. The tracked artifacts in this directory are
generated by `scripts/materialize_adam_soft_lambda_redo.py`; bulk model
checkpoints remain in `_artifacts/`.

The tracked `adam_soft_lambda_redo.json` is a slim manifest: it keeps the
summary/manifest fields and a `bulk_detail_manifest` pointer (repo-relative
`_artifacts` path + sha256 + counts). The canonical nested per-run Adam rows
(`rows[].adam_rows`, ~890 KB) live in the gitignored bulk detail file
`_artifacts/d469108/adam_soft_lambda_redo_detail.json`, and the flat per-setting
table is the CSV twin `adam_soft_lambda_redo.csv` (the previously duplicated
`flat_rows` array is no longer serialized into the tracked JSON). Issue `7555cdb`.
