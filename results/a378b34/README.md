Issue a378b34 owns the no-launch preflight for pure closed-loop extLQG distillation into the standard h0 C&S GRU. The row uses the 6D extLQG analytical teacher from issue 376d023 and the h0 force-filter feedback graph contract from issue 020a65b. The local trainer path is implemented through the normal Feedbax `TaskTrainer`/`train_pair` closed-loop rollout with a custom `ClosedLoopDistillationLoss`; any full/billable run remains approval-gated by the locked run spec.
