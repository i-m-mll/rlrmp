# Perturbation calibration

This directory tracks the perturbation-amplitude calibration work for C&S GRU
evaluation and training. Calibration should define perturbation-strength bins by
nominal open-loop extLQG command-replay peak `delta x`, then report closed-loop
extLQG and nominal-only vanilla GRU responses at the same amplitudes.
