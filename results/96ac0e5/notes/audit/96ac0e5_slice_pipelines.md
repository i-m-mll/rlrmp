# Audit slice: `src/rlrmp/analysis/pipelines/` (issue 96ac0e5)

Worktree: `/Users/mll/Main/10 Projects/10 PhD/rlrmp/worktrees/integration__d1f90ac-defaults-cascade`
Excluded (already covered elsewhere): `output_feedback_phase_modulated_recurrent.py`, `cs_stochastic_phase1.py`, `_selected_eval_rollouts.py`.

Reference surfaces read: `feedbax/contracts/expressions.py` (Compare/AllOf/AnyOf/Not, Select exact-cardinality,
Coerce, ValueQuery), `feedbax/contracts/extraction.py` (ExtractionProductSpec: fixed-URI SourceBinding +
FieldMapping(output_path, ValueQuery), fail-closed on missing source, no derived-value computation),
`src/rlrmp/config/analysis_bundles/feedback_quality_lens.yml` (bundle `run_condition`/`depends_on_roles`
= `Compare`/`AllOf`/`AnyOf` gates over named items, evaluated per-stage before execution).

## A/B/D sites

| site | file:lines | what it does | sources read | classification | computation? | blocker/rationale |
|---|---|---|---|---|---|---|
| 1 | `gru_map_error_decomposition.py:495-507` `_find_standard_row` | Loops `manifest.get("rows", ())`, finds the row where `row.spec.parameters.source_run_id == run_id`, returns a **renamed** subset (`run_id`←`spec.run_id`, `status`, `observation_history_to_action_map_mismatch`←nested component lookup) | `manifest` opened at line 52 via `_read_json(standard_manifest_path)`, a tracked `results/...` JSON | **A** | No — pure eq-match + rename | None; textbook `ValueQuery(path="rows", select=Select(where=Compare(item="entry", path="spec.parameters.source_run_id", op="eq", value=run_id)))` + `FieldMapping` renames |
| 2 | `gru_map_error_decomposition.py:510-518` `_component_summary` | Loops `row.get("certificate_components", ())`, finds entry where `component.name == component_name`, returns renamed `status`/`summary`/`reason` | Same tracked-JSON `manifest`, nested list | **A** | No | Same as above; nested `Select` inside the row already selected by site 1 |
| 3 | `gru_feedback_ablation.py:559-655` `feedback_checkpoint_selection_audit` | Filters `manifest["runs"]` to per-run `feedback_checkpoint_rescore` entries with `status=="materialized"` (Compare/isinstance-eligible); else falls through to a `candidates` list built from `.get()` chains and picks `max(candidates, key=lambda c: c["feedback_score"])` | `manifest` (in-memory `Mapping`, caller-supplied — not opened in this function) | **D**, filter half is A-shaped | Argmax over a numeric field is COMPUTATION for selection purposes | `Select` requires exact-cardinality boolean match, not ranking; no argmax/argmin primitive in the grammar |
| 4 | `gru_feedback_ablation.py:276-308` `_select_representative_perturbation_id` | Filters candidate rows by `family`/`timing_bin`/`units`/optional `level_name` (all Compare-eligible AllOf), then breaks ties with `min(candidates, key=sort_key)` where `sort_key` computes a derived `(axis_priority, sign_priority, id)` tuple | In-memory `rows: Mapping[str, Mapping]` | **D** | Yes — priority-tuple derivation | Filter alone would be `Select`-eligible only if cardinality were guaranteed 1; disambiguation needs computed ranking, unsupported |
| 5 | `gru_feedback_ablation.py:515-556` `summarize_feedback_pass_audit` | Combines 5 component statuses into one aggregate via priority order `fail > not_available > warn > pass` | In-memory `components` dict | **D** | Yes — priority-ranked string aggregation is a derived value | `AllOf`/`AnyOf` return booleans only; can't produce a ranked enum |
| 6 | `gru_checkpoint_selection.py:1580-1609` `load_materialized_fixed_bank_manifest` | Picks the **first existing** path among an ordered list of 2 candidate tracked-JSON paths (`next(... if path.exists() ...)`), loads it, then gates on `checkpoint_policy == FIXED_BANK_CHECKPOINT_POLICY` and `materialization_status == "materialized"` (both Compare-eq) | Two candidate `results/`-relative JSON paths | **D** for path resolution, **B**-shaped for the post-load gate | No, for the gate; path-choice itself is not computation, just unsupported combinator | `ExtractionProductSpec.SourceBinding.uri` is one fixed repo-relative URI — no "first of N candidate paths" fallback-coalesce primitive |
| 7 | `gru_checkpoint_selection.py:721-780` `select_validation_checkpoints_for_run` | Mode-string dispatch (`sparse_history` / `fixed_bank_manifest`) plus `run_id in manifest.get("runs", {})` existence check, routing to different function calls | `effective_manifest` (loaded via site 6) | **D** | No | This drives **internal Python control flow** (which function to call), not a stage-level `run_condition` gate — the grammar targets external stage eligibility, not in-function branching |
| 8 | `hinf_phenotype_sidecar.py:96-122` `load_hinf_phenotype_sources` | For each named source path: if `None` → `loaded[name]=None`; if `not path.exists()` → status=`"missing"` wrapper; else reads JSON and wraps `{"status": "available", "source_path":…, "payload": …}` | N tracked JSON manifests via `source_paths: Mapping[str, Path|None]` | **B**/A hybrid | No | Existence check is `Compare(op="exists")`-eligible per source, but `ExtractionProductSpec` sources are fail-closed (`FileNotFoundError` if missing) — the graceful "degrade to status=missing" semantics has no analogue in the extraction contract |
| 9 | `gru_postrun_materialization.py:1119-1141` `fixed_bank_rescore_manifest_status` | Opens tracked JSON at `path` (existence-gated → `"missing"` else read); extracts/renames `materialization_status`→`status`, plus `schema_version`, `validation_bank`, `not_materialized_reason`; also derives `selection_use` via a two-way string ternary keyed on `materialization_status=="materialized"` | One tracked `results/...` JSON | **A** (renamed fields) split from **D** (`selection_use`) | Only for `selection_use` (ternary substitution, no such combinator in `Coerce`) | Straightforward `FieldMapping` renames for 4 fields; `selection_use` needs an if/else value substitution the grammar doesn't have |
| 10 | `gru_postrun_materialization.py:810-839` `feedback_checkpoint_selection_status` | Early-return gate on `status != "materialized"`; `isinstance(audit, Mapping)` gate; then renames `audit.status`→`status`, `schema_version`, `primary_checkpoint_policy`, conditionally copies `reason`/`selected_candidate` only if present | In-memory `feedback_ablation: Mapping` (not opened here) | **D** (in-memory source) though internal shape is Select/Compare-like | No | Category-A-shaped logic, but the source is an in-memory payload assembled upstream, not a `repo-relative tracked-JSON URI` — outside `SourceBinding`'s scope as written |
| 11 | `gru_postrun_materialization.py:842-909` `materialize_optional_perturbation_response` | `try: importlib.import_module(...)` / `except (ImportError, AttributeError)` → status=`"skipped"`; second `try/except (FileNotFoundError, ValueError, KeyError, AttributeError)` around calling the materializer | N/A (dynamic import + call) | **D** | No | Availability is code/import-driven, not data-driven — no Expr can express "does this module import cleanly" |
| 12 | `objective_comparator.py:1100-1121` `load_run_objective_metadata` | `try: resolve_run_record(...) except RunSpecValidationError: return _missing_run_metadata(...)`; else extracts/renames `loss_objective`, `objective_profile`←`loss_summary.objective_profile`, `full_qrf_lens`←derived | Tracked run-spec JSON via `run_spec_path(...)` | **B**(gate)/**A**(renames), split | Gate is exception-driven (no) | Try/except around a schema-validating resolver isn't a payload predicate — Expr only evaluates already-loaded context, can't validate the load itself |
| 13 | `objective_comparator.py:1124-1139` `_full_qrf_lens_from_loss_summary` | `status` = `"available"` iff `{"state_running_q","terminal_q_f","control_r"}.issubset(active_terms)`; renames `state_basis`, `time_indexing`, `matrix_shapes`, `force_filter_state_cost`, `disturbance_integrator_state_cost`; `active_terms`←`sorted(str(t) for t in active_terms)` | Same `loss_summary` mapping | **B** for the subset gate, **A** for renames, **D** for `active_terms` | Only for `active_terms` (sort+stringify) | Subset-of-3-keys gate = `AllOf` of three `Compare(op="exists")`; `active_terms` sort/stringify has no `Coerce` equivalent |
| 14 | `gru_broad_epsilon_attribution.py:374-394` `discover_broad_epsilon_run_ids` | Globs `results/<experiment>/runs/*.json`, resolves each run spec, filters on `broad.get("enabled") is True` and `not run_id.startswith("smoke__")` | Dynamically discovered set of tracked JSON run specs (`run_root.glob("*.json")`) | **D** | No | `SourceBinding` needs a fixed, enumerated `uri` per source; a directory glob that determines *which* sources exist has no representation in the extraction/predicate grammar |
| 15 | `gru_perturbation_bank.py:1050-1062` `_load_feedback_scale_manifest` | Mutually-exclusive `manifest` vs `manifest_path` args (raises if both given), else reads tracked JSON at `manifest_path` if present | One tracked JSON, alternatively supplied pre-parsed | **D** | No | Mutual-exclusivity validation is exception-raising control flow, not boolean gating; also two alternative *source kinds* (object vs URI), unsupported |
| 16 | `gru_perturbation_bank.py:1065-1099` `_controller_feedback_component_scale` | Loops `scale_entries`, guards `component not in components` / `not isinstance(payload, Mapping)`, then `payload.get("reference_scale", payload.get("p95_norm"))` two-level fallback-coalesce | Manifest already loaded (via site 15) | **B** for the membership/type guards, **D** for the coalesce | No | The `get(a, get(b))` fallback-coalesce is explicitly the named grammar gap — no "try path A, else path B" combinator in `ValueQuery` |
| 17 | `gru_perturbation_calibration.py:965-980` `_selected_calibration_rows` | Filters rows on `row_kind=="reach_relative_calibrated_amplitude"` AND `open_loop.status=="available"` (both Compare-eq), then `sorted(selected, key=lambda row: (4-tuple of coerced fields))` | In-memory `rows: Sequence[Mapping]` | **D** | Yes, for the sort (multi-key float coercion + tuple ordering) | Filter is a clean `AllOf(Compare, Compare)`, but it must return **N≥1** matches — `Select` only supports exact-cardinality-1; no generic multi-match filter primitive exists |
| 18 | `gru_perturbation_response_norm_plots.py:307-345` `load_plot_inputs` | Opens tracked manifest JSON; per-run filters on `run_id_contains` substring-any (`AnyOf(Compare contains)`-eligible); per-perturbation-row filters `status != "evaluated"` (skip), `severity not in SEVERITY_ORDER`, `timing_bin == "not_applicable"` (skip) | Tracked `results/...` manifest JSON (or a `bulk_detail_manifest` sidecar) | **D** overall (raw gates B-shaped) | Yes — `severity`/`timing_bin` are computed via `_severity(row)` / `_normalized_timing_bin(...)` before the gate runs | The eligibility check needs a **derived** value the grammar cannot compute; Expr only compares already-present fields |
| 19 | `output_feedback_rollout_recovery.py:1355-1399` | Mutual-exclusion + exists-based dispatch: `condition.eigenspectrum_coverage is not None` vs `condition.observer_error_coverage is not None` selects which large computation branch runs | In-memory dataclass `condition` (not tracked JSON) | **D** | N/A (dispatch, not extraction) | Source is an in-memory config object, not a tracked-JSON URI — outside `SourceBinding`'s scope by definition |
| 20 | `bridge_contracts.py:296-299` `read_bridge_manifest` | Full round-trip deserializer: `BridgeRunManifest.from_json_dict(json.loads(path.read_text()))` reconstructs the **entire** typed manifest object, no selection | Tracked bridge-manifest JSON | **D** | No, but it's whole-object reconstruction, not selection | Not what `ExtractionProductSpec` targets (building a *new*, smaller governed product from selected fields) — this is symmetric full manifest serde |
| 21 | `cs_gru_standard_materialization.py:321,421` | `training_summary = _read_json(path) if path.exists() else {}`, then the **entire** dict is forwarded unselected into `build_gru_standard_manifest_from_actions(training_summary=training_summary, …)` | Tracked `training_summary.json` | **D** | No | Whole-payload passthrough, not hand-selection; the existence gate (`if path.exists() else {}`) is the only Compare-shaped piece and is trivial |
| 22 | `gru_worst_case_epsilon_audit.py:1210-1243` `render_worst_case_epsilon_markdown` | Status-gate (`row.get("status") != "evaluated"`) + field extraction, but the destination is a **Markdown string**, not a JSON parameter payload | Manifest already in memory | **D** | No | `ExtractionProductSpec`/`FieldMapping` target JSON `AnalysisDataProduct` parameters, not prose rendering — out of the contract's output shape by construction (same pattern recurs in `hinf_phenotype_sidecar.render_hinf_phenotype_markdown` and similar render functions across this slice; not re-tabulated per occurrence) |
| 23 | `diagnostic_provenance.py` (whole file) | Git subprocess calls, SHA-256 file/tree hashing, byte-count summaries — all genuinely computed provenance data | Filesystem tree walks (`rglob`), not tracked JSON | **D** / out of scope | Yes, throughout | Everything here is derived (hashes, sizes, git state); no selection-from-existing-JSON pattern at all |
| 24 | `gru_steady_state_perturbation_bank.py:601` | `next(iter(condition_payloads.values()))["dt_s"]` — picks an arbitrary first entry (all entries share the value) | In-memory dict built earlier in the same function | **D** (trivial) | No | In-memory, arbitrary-first-entry pick; not a tracked-JSON source and not a meaningful selection distinction |

## Files surveyed with no clear A/B candidates found

- `bridge_certificates.py`, `gru_evaluation_diagnostics.py` — certificate/kinematic numerics over in-memory arrays; no manifest hand-selection.
- `feedbax_parity.py` — pure plant/GraphSpec construction; no JSON reads at all.
- `standard_certificate_materialization.py` — in-memory typed-object indexing/aggregation (`Counter`, dict-by-name rekeying), not tracked-JSON extraction.
- `bridge_controllers.py`, `sisu_spectrum_diagnostics.py`, `gru_pilot_figures.py` — surveyed via targeted grep (low `.get(`/bracket-index hit counts, no `json.load`/`elif`/`except`-as-control-flow signal); not read in full given slice scope — flagging for a follow-up pass if a deeper sweep is wanted.
- `gru_checkpoint_selection.py` scoring functions (`score_fixed_bank_checkpoints_for_run`, `select_sparse_history_validation_checkpoints_for_run`) — numpy argmin over freshly-computed validation-objective arrays from `.eqx`/binary training history, not JSON selection.

## LEGACY-skipped (module-level banner, per CLAUDE.md convention)

All bear the identical banner `"""LEGACY (frozen 2026-07-03, issue 64d5f13). This materializer is not contract-native: it predates the feedbax recipe, bundle, and manifest contracts. It may not run without deliberate realignment.`:

- `bridge_aggregation.py`
- `cs_stochastic_phase3.py`
- `delayed_diagnostic_bundle.py`
- `failure_decomposition.py`
- `output_feedback_affine_tracker.py`
- `output_feedback_interpolated_starts.py`
- `output_feedback_linear_recurrent.py`
- `output_feedback_time_constrained.py`
- `sisu_perturbation_comparison.py`

## Category counts

- **A** (clean extraction/ValueQuery candidates, no blocking computation): 2 sites outright (1, 2), plus 2 split-eligible halves (9, 12/13 rename portions)
- **B** (predicate/gating candidates expressible via Expr/run_condition): 0 clean standalone sites; 6 sites are B-shaped in part but paired with a blocker (3, 6, 8, 10 partial, 12, 13, 16 partial, 18 partial) — no site is purely B with zero complication
- **D** (borderline/blocked): 20 sites — dominant category in this slice. Recurring blockers: argmax/argmin selection with no ranking primitive (3, 4, 6-partial-not-really/17-conceptually similar), fallback-coalesce chains across multiple candidate paths or fields (6, 15, 16), derived/computed values gating or populating the payload (5, 9, 13, 17, 18), dynamic filesystem discovery (14) or code-availability (11) instead of fixed-URI sources, in-memory (not tracked-JSON) sources (7, 10, 19, 24), whole-object passthrough/round-trip rather than selection (20, 21), and markdown-destination rendering rather than JSON products (22).
- **LEGACY-skipped**: 9 files (module-level banner)
- **No-candidate files**: 7 files surveyed with no A/B/D sites worth tabulating

## Top-3 highest-value migration targets in this slice

1. **`gru_map_error_decomposition.py:495-518`** (`_find_standard_row` + `_component_summary`) — the cleanest possible case in the whole slice: a manifest opened from tracked JSON, a single-field eq-match `Select`, and pure field renames with **zero computation**. This is a direct, low-risk `ExtractionProductSpec`/`ValueQuery` port.
2. **`gru_postrun_materialization.py:1119-1141`** (`fixed_bank_rescore_manifest_status`) — 4 of 5 output fields are clean renames from a tracked JSON manifest with a simple existence gate; only the `selection_use` ternary needs to stay as a small wrapper (or motivates a `Coerce`-style value-substitution extension to the expression grammar).
3. **`gru_checkpoint_selection.py:1580-1609`** (`load_materialized_fixed_bank_manifest`) — once loaded, the post-load eligibility gate (`checkpoint_policy` eq, `materialization_status` eq) is textbook `AllOf(Compare)`; the "first existing of N candidate paths" resolution is the one piece worth flagging as a candidate `SourceBinding` extension (an ordered fallback-path list) rather than blocking migration outright — a good design-discussion seed for the fallback-coalesce gap that recurs across sites 6/15/16.

File: `/private/tmp/claude-501/-Users-mll-Main-10-Projects-10-PhD-rlrmp/08a431d4-ec60-4f13-ab0e-b9622d4fe1d8/scratchpad/audit/96ac0e5_slice_pipelines.md`
