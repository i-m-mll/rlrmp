All coverage is complete. Final combined report follows.

## Category A: extraction/ValueQuery candidates (analysis/ top-level + math/ + matrix/)

All paths relative to `/Users/mll/Main/10 Projects/10 PhD/rlrmp/worktrees/integration__d1f90ac-defaults-cascade/src/rlrmp/analysis/`.

**Scope note on strictness:** in `math/` and `matrix/`, no live code directly `open()`s/`json.load()`s a tracked file under `results/` — the only literal tracked-file reads sit inside function-scoped LEGACY surfaces (skipped). The near-A rows below operate on manifest-derived in-memory payloads where feedbax did the file read upstream; the top-level `analysis/` rows include genuine artifact-URI JSON reads.

| site | file:lines | what it does | sources read | classification | computation? | blocker/rationale |
|---|---|---|---|---|---|---|
| `_load_perturbation_class_product` | declarative_materialization.py:2120-2139 | Selects the one artifact with `role=="rlrmp-perturbation-class-response"` from `manifest.artifacts`, reads JSON at `artifact.uri`, validates `schema_id` equality | JSON at artifact URI (results-tree analysis-manifest artifact) | A (strong) | no | Near-direct `Select(where=Compare(role, eq, ...))` + `Compare(schema_id, eq, ...)` |
| `_feedback_quality_component_outputs_from_inputs` | declarative_materialization.py:1696-1720 | Reads JSON payloads for artifacts matching `(role, logical_name)` pairs from a static registration table across multiple resolved manifests; renames into `outputs[name]`; raises if any component missing | JSON at artifact URIs | A (multi-select) | no | "Select N named items from a pooled list" exceeds one-item `Select`; feasible with per-component nested selects |
| `_robustness_phenotype_source_paths` | declarative_materialization.py:1860-1880 | Merges `params["source_paths"]` overrides with role→name lookups over each input's `manifest.artifacts`; extracts + renames `artifact.uri` | manifest artifact list | A (strong) | no | Pure selection/rename |
| `_perturbation_class_response_payload` + slicing helpers | declarative_materialization.py:1883-2117 (slicers 1948-2055) | Selects perturbation rows by requested `row_ids` from `class_index_map`/`perturbation_battery`/`response_tensors`; slices/renames (`class_response_row_index`) | evaluation-manifest-backed states payload | A (mixed) | **yes** | `_slice_perturbation_run` (2046-2047) computes `status_counts` and calls `summarize_perturbation_bank` (aggregation) — blocks pure ValueQuery; row selection/slicing itself is clean |
| `_aggregate_perturbation_class_products` / `_aggregate_bank_rows` / `_aggregate_leaf_runs` | declarative_materialization.py:2142-2221 | Validates single shared `evaluation_manifest.id` across leaves, copies/renames `aggregate_base`, concatenates + sorts + strips index key across leaf JSON payloads | leaf JSON payloads | A (mixed) | **yes** | List concatenation + repeated `status_counts`/`robust_response_summary` recompute (2219-2220) is real aggregation, out of ValueQuery scope |
| `_postrun_existing_artifacts` | declarative_materialization.py:2403-2461 | Walks static `direct_paths`/`output_roles` tables, indexes `manifest["outputs"][name][key]`, extracts/renames into `(role, path)` pairs | analysis manifest `outputs` dict | A (table-driven) | no | Gated by filesystem `path.exists()` — see Category D `exists`-semantics note |
| `_feedback_quality_components` / `_component_paths` | declarative_materialization.py:2468-2616, 2679-2687 | Per-component static dict of path/role tuples, each choosing `params.get(override) or plan.default` | params + plan defaults | A (config-driven rewrite candidate) | no | Override-or-computed-default coalescing isn't pure `ValueQuery` (no coalesce combinator) |
| `_bulk_artifact_groups` | declarative_materialization.py:2690-2726 | Iterates `manifest.get("runs", {})`, filters to entries with a `bulk_arrays` mapping + existing path, builds renamed `AnalysisArtifactFile` records | manifest `runs` dict | A | no | Simple filter+select+rename, aside from disk-existence gate |
| `_section_for_artifact` (extraction half) | reports.py:245-276 | Reads artifact file at `artifact.uri`; extracts/renames `role`/`logical_name`/`media_type`/`sha256`/`uri` into a section dict; loads JSON body under `"json"` or text under `"text"` | artifact file at `artifact.uri` (feedbax-custody) | A | no (gating conditional) | Field selection/rename is pure; the json-vs-text branch + derived `status` is the Category B half |
| `summarize_active_broad_epsilon_optimizer` passthrough fields | frozen_adversary_audit.py:669-700 | ~20-key summary dict extracting/renaming nested run-spec fields (`hps.broad_epsilon_pgd_training.*`, `hps.policy_adversary_training.*`; `objective.get("kind")` → `objective_kind`) | `run_spec: Mapping` (tracked `run.json`/training manifest) | A (large) | partially | `active_lane`/`active_method`/etc. are outputs of the Category B gating in the same function; only the straight passthroughs are pure extraction |
| `_resolve_regeneration_spec_path` | rollout_cleanup.py:224-236 | Extracts `manifest.get("regeneration_spec")`, resolves repo path; conventional-path fallback when absent | tracked manifest at `results/&lt;issue&gt;/notes/*_manifest.json` | A (with fallback) | no | Single-value `ValueQuery` shape, but needs override → field → conventional-default fallback; no "OR default" combinator |
| `_resolve_bulk_dir` (manifest branch) | rollout_cleanup.py:188-202 | Extracts `manifest["bulk_detail_manifest"]["path"]`, resolves parent dir | tracked manifest | A (partial) | no | Simple path-walk embedded in an if/elif/raise chain (see Category B) |
| `validate_direct_hvp_lambda_source` beta-row select | frozen_policy_gate.py:279-284 | Loops `payload.get("pooled_beta_mapping", [])`, matches `float(row["beta"]) == float(beta)`, raises if none | caller-supplied payload from tracked frozen-audit JSON (per docstring, "from 06a4dc8") | A (clean Select) | no | Pure `Select(where=Compare("beta", eq/approx_eq))` |
| `validate_direct_hvp_lambda_source` field extraction | frozen_policy_gate.py:266-302 | Pulls `estimator`/`objective_contract`/`pooled_summary`; extracts `lambda_star_p90`, `schema_version`, `issue`, `method`; renames into output keys | same payload | A (mostly pure) | no | Blocked by embedded gating raises (Category B) and one synthesized fallback string when `mapping` absent |
| `PerturbationSpec.from_mapping` | perturbation_rows.py:148-199 | Required-field validation; ~20 fields extracted with per-field str/int/float/bool coercion; residual `extra` dict via set-difference | wire perturbation-bank row (Mapping) | A (FieldMapping+Coerce shaped) | no | Dynamic `extra` catch-all needs a "residual passthrough" construct `ExtractionProductSpec` lacks |
| `_summarize_training_diagnostics` manifest/summary pulls | training_diagnostics.py:159-167 | Reads `completed_batches` / `stopped_early_for_checkpoint_gate`, renames into output keys | `&lt;output_dir&gt;/training_diagnostics.json`, `training_summary.json` | A (clean) | no | Pure select+rename; pairs with existence gating (Category B) — both files optional |
| `_experiment` fallback/coalesce | training_diagnostics.py:366-372 | `params["experiment"]` else `params["figure_routing"]["experiment"]` else literal default | analysis-recipe params | A (near-ideal) | no | Only `str()` coercion; needs default-coalesce combinator |
| `_first_artifact` role-match select | training_diagnostics.py:329-335 | First `ArtifactRef` whose `.role` is in a role set, else `None` | `resolved.manifest.artifacts` (TrainingRunManifest) | A (textbook `Select` + `in`) | no | Operates on typed dataclasses, not dicts — needs `ContextItem` to bind non-dict objects |
| `standard_matrix_evaluation_recipe` fallback-cells build | matrix/standard_matrix.py:122-133 | Builds `cells` from `run_spec.inputs`: `ref.id` → `run_id`, `ref.metadata[label/display_name/color]` → cell fields with defaults | `EvaluationRunSpec.inputs` refs (manifest-derived) | A (near) | no | Per-entry mapping over variable-length list + default-coalescing exceed `ValueQuery` |
| `_cells_from_inputs` + `_merge_cell_metadata` | matrix/standard_matrix.py:181-249 | Normalizes manifest-resolved states into cell dicts; renames/coalesces `run_id`/`label`/`display_name`/`color` across three sources with merge precedence | `ResolvedAnalysisInput.states` + `resolved.manifest.metadata` | A (near) | no | Multi-source `or`-coalescing chains (233-248) + merge precedence exceed select/rename/coerce |
| `_metric_value` | matrix/standard_matrix.py:486-492 | Path-walk `cell.summary_metrics.&lt;metric&gt;` with top-level fallback, float coercion | manifest-derived cell payload | A (near) | no | Exactly `ValueQuery(path, coerce=float)` plus fallback path + `isinstance` guard |
| `_figure_params` | matrix/standard_matrix.py:261-266 | Subset-key selection (`figure_routing`, `presentation`, `metric_order`, `profile_key`) into new payload | recipe params | A (trivial) | no | Fully expressible with `exists`-gated field mappings |

## Category B: predicate/gating candidates (analysis/ top-level + math/ + matrix/)

| site | file:lines | what it does | sources read | classification | computation? | blocker/rationale |
|---|---|---|---|---|---|---|
| Feedback-quality component gating | declarative_materialization.py:1407-1470, 2619-2676 | Eligibility cascade: `params.get(f"include_{name}", True)` flag, `not_applicable` set-membership, `status=="unavailable"` re-materialize branch — duplicated across materialize and output paths | params + component status dict | B (strongest in sweep) | no | Each `FeedbackQualityComponentRegistration` already carries a `gating_spec` **string** naming its intended condition (e.g. `"evaluation_run_present_or_materialize_flag"` at 1347/1358/1369/1380/1391/1402) while the actual gating stays procedural — the declarative shape is documented but unimplemented; direct `run_condition` analogue of what `feedback_quality_lens.yml` already declares |
| `_materialize_gru_standard` states branch | declarative_materialization.py:1122-1149 | `if evaluation_states is not None:` selects between two materializer implementations | resolved evaluation input | B (clean) | no | `Compare(op="exists")`-shaped |
| `_materialize_gru_standard` note/manifest branch | declarative_materialization.py:1153-1193 | `if note_path is not None or manifest_path is not None:` selects legacy write+read-back vs direct-result strategy | params | B/D | no | Both arms carry heavy side effects — heavier than an include/skip toggle |
| `_run_ids_from_params_or_inputs` / feedback-quality variant | declarative_materialization.py:2301-2344 | if/elif dispatch on `ref.kind` (`EvaluationRunManifest` vs `TrainingRunManifest`) choosing harvesting strategy | resolved input refs | B (dispatch) | **yes** (in branches) | Branch selection is `AnyOf(Compare(kind, eq, ...))`; per-branch list-building is out of scope |
| `_identity_matches` / `_calibration_identities_for_leaf` | declarative_materialization.py:2084-2085, 2058-2081 | Reimplements `AllOf(Compare(eq))` via `all(recorded.get(k)==v ...)`; caller gates on `any(...)` before raising | states + params | B (clean) | no | Exactly `AnyOf(AllOf(Compare(eq)))`; normalization resembles `Coerce` |
| `_experiment_from_params_or_inputs` | declarative_materialization.py:2347-2366 | First-match-wins: `params["experiment"]`, else first input with `metadata_key in metadata`, else raise | params + input metadata | B/D | no | Priority-ordered first-match returning the **value** isn't a documented combinator (`AnyOf` returns bool) |
| Active-lane + warnings logic | frozen_adversary_audit.py:626-668 | if/elif/else on `broad_enabled`/`policy_enabled` selecting `active_lane`/`active_method`/`active_mechanism`; warning appends keyed on `Compare`-shaped checks (`!= "adam"`, set membership, nested equality, flag absence) | run-spec Mapping | B (strong) | no | Conditions map to `Compare(eq/in/exists)`/`AllOf`/`AnyOf`; free-text warning strings are outside the AST — each `Expr` would need a fixed message keyed to which fired |
| `_section_for_artifact` status cascade | reports.py:245-276 | if/elif over `uri is None` / `path.exists()` / media-type membership → status enum (`missing_uri`/`missing_artifact_file`/`invalid_json`/`unsupported_media_type`/`materialized`) | artifact file | B | no | `exists`/`has_type`-shaped, but `json.JSONDecodeError` try/except is control-flow-as-validation — needs a "parses as valid JSON" op the vocabulary lacks |
| Manifest/role filter | reports.py:170-183 | `if manifest is None:` guard, then filters `manifest.artifacts` by `role not in source_roles` | resolved report input | B (filter-many) | no | `in`-style `Compare`, but `Select` picks exactly one; filter-to-many idiom has no combinator |
| Resolver override chains (×4) | rollout_cleanup.py:188-255 | Each of `_resolve_bulk_dir`/`_resolve_regeneration_spec_path`/`_resolve_summary_paths`/`_resolve_manifest_out_path`: explicit param wins, else manifest field, else conventional default, else raise | tracked manifest + overrides | B (fallback chain) | no | Four near-identical hand-rolled chains in one file — missing reusable declarative coalesce combinator |
| Cleanup precondition filters | rollout_cleanup.py:89-99 | `missing = [item for item in required_artifacts if not item["exists"]]` + `missing_manifest_references` existence filters | records + manifest-derived paths | B (cleanest in file) | no | Directly `Compare(op="exists")` filtered over a list |
| Lambda-source guard checks | frozen_policy_gate.py:267-274 | `cap_or_interiority_used_as_criterion is not False` → raise; lowercased substring checks on `safety_cap_role` | payload | B | no | `Compare(ne)` + `Not(AnyOf(contains))`; `.lower()` normalize-then-contains has no combinator — partial blocker |
| Metric alert thresholds | training_diagnostics.py:414-425 | `ratio_max &gt; 1.0001` / `gap_min &lt; -1e-6` on nested `.get()` chains, setting `ok=False` + formatted alert | result dict (from `.npz` sidecar) | B | no | `Compare(gt/lt)` directly expressible; message templating with interpolated values is out of scope |
| `_param_output_dir` fallback chain | training_diagnostics.py:320-326 | `isinstance(Mapping) and run_id in output_dirs` → value; else `"output_dir" in params` → value; else `None` | params | B | no | Value-resolution fallback (not pure gate); `isinstance(Mapping)` exceeds `has_type` scalar vocabulary |
| `_read_json` optional-existence gate | training_diagnostics.py:428-431 | `if path is None or not path.exists(): return None` before `json.loads` | tracked JSON path | B (clean) | no | With downstream `if manifest is not None:` field-population, forms a clean `run_condition`-style pair |
| Channel rule dispatch | perturbation_rows.py:201-223 | `_CHANNEL_FIELD_RULES.get(channel)` lookup + family membership + required-timing-field raise cascade | spec + rule table | B (low value) | no | Already a declarative Python rule table; migration mostly relocates it. Per-row schema validation, not bundle-stage gating — no conflict with `feedback_quality_lens.yml` |
| `hps` cost-schedule digging cluster | math/hinf_riccati.py:1412-1471 | `getattr`/`.get` traversal with `is None` fallbacks over `hps.loss.weights` and `hps.loss_update`; branches on `bool(getattr(loss_update, "enabled", False))` to pick R_weight source | in-memory `hps` TreeNamespace (built upstream from tracked config) | B | **yes** | Lookups are ValueQuery/Coerce-shaped, but outputs multiply extracted fields (e.g. `pos_mid_weight = _w(...) * mid_pos_final`) — arithmetic blocks pure extraction; only branch selection is delegable |
| `_resolve_go_step` epoch-range gate | math/hinf_riccati.py:1476-1490 | `if epoch_ranges is None:` conservative default else `sum(r[1]-1 ...)` | `hps` namespace | B | **yes** | Gate is a clean `Compare(exists)`; both branches end in computation |
| `loss_update.enabled` branch | math/hinf_riccati.py:1449-1457 | Boolean-flag-gated selection of `converged_R` vs prior `_w("nn_output", 1e-6)` value | `hps` namespace | B | no | Textbook `Compare(eq)`/select shape; else-value comes from an earlier call, not a local literal |
| Legacy-payload-mode gates | matrix/standard_matrix.py:115-135 | `legacy_payload_mode is True` flag + `params.get("matrix_payload", params.get("states"))` fallback; raises payload-without-mode / mode-without-payload | `run_spec.params` | B (strong) | no | Textbook `AllOf(Compare(exists), Not(Compare(eq true)))` fail-closed validation |
| Schema-key cascade | matrix/standard_matrix.py:150-154 | `"schema_version" not in params and "schema_id" not in params` → passthrough vs migration | params | B | no | `Not(AnyOf(exists, exists))`; migration call stays imperative |
| `_normalise_cells` shape dispatch | matrix/standard_matrix.py:207-215 | if/elif over payload type: Mapping-with-`"cells"` / Sequence / bare Mapping / raise | states payload | B (partial) | no | Gating half is `has_type`/`exists` territory; list-wrapping normalization isn't |
| Gamma-sweep eligibility filter | math/output_feedback.py:1855-1863 | 4-way AND filter (`feasible` + three `_min_eig &gt; 0`) → `eligible`, then `min(eligible, key=...)` recommendation | in-memory rows (become the tracked gamma-sweep manifest downstream) | B | **yes** (argmin) | Filter is `AllOf(Compare)`; argmin isn't expressible by `Select` |
| try/except-as-control-flow per gamma | math/output_feedback.py:1725-1852 | Blanket `except Exception` → `row["status"]="failed"` around ~130 lines of computation | n/a | B (pattern match) | **yes** | Matches the brief's pattern though guarding computation, not a file read; declarative admissibility precondition could replace catch-all |
| `estimator_kind` dispatch (×3) | math/output_feedback.py:604-619, 632-646, 690-706 | `== "kalman"` / `== "robust"` / raise, repeated three times in one function | function arg | B/D | no | `Compare(eq)`-shaped but selects a computation branch, not a run/skip gate |
| Feasibility gate nulling fields | math/robust_bellman.py:1469-1498 | `feasible = final_margin &gt; tolerance` conditionally nulls objective/ratio/gain-error and swaps `optimizer_status` | in-memory fit | B | no | Hand-implements the repo's "not_applicable when infeasible" convention; single-`Compare` gate |
| Existence-gated gain-error dict | math/robust_bellman.py:1235-1245 | `if fit.K is not None: {...} else None field` | in-memory fit | B | no | `Compare(exists, "K")` shaped |
| exists-filtered aggregates | math/robust_bellman.py:1143-1147, 1246-1248, 1253-1261 | Filter by `is not None`, then null-guarded `max`/`mean` | fits/gains | B (partial) | **yes** | Filter is B-shaped; reduction blocks pure declarative query |
| Recovery booleans | math/robust_bellman.py:1154, 1280-1282 | `bool(x is not None and x &lt; 2e-2)` | in-memory | B | no | `AllOf(Compare(exists), Compare(lt))` — produces a value, not an execution gate |
| `all_feasible` aggregation (×2) | math/robust_bellman.py:1158, 1288 | `all(fit.feasible for fit in fits)` | fits | B | no | `AllOf([Compare(eq true)...])` |
| Phase-3 pass-band status chain | math/linear_round_trip.py:667-684 (+715-733) | Threshold `&lt;=` comparisons → 3-way `phase3_status`; `teacher_pass` two ANDed `&lt;= 0.01`; nested conditional selects interpretation prose | computed results vs hard-coded tolerances | B (classification) | no | `AllOf(Compare(le))`-shaped but produces labels. **Drift risk:** tolerances stated twice — in predicates and again as data in `predeclared_tolerances` (~734-739); a declarative gate would collapse the duplication |

## Category D: borderline (analysis/ top-level + math/ + matrix/)

All rows tagged **borderline**.

| site | file:lines | what it does | classification | rationale |
|---|---|---|---|---|
| Pervasive `params.get(key, default)` + manual `bool()/str()/int()` coercion | declarative_materialization.py:~563-861, ~863-1092, 1204-1334, 1491-1671 | Every spec-builder and materializer wrapper hand-digs typed defaults from `AnalysisRunSpec.params` | borderline | Structurally `Coerce(to=...)` on a runtime params dict, not a tracked JSON source — the Python-side cousin of the YAML `run_condition` pattern (c14382f precedent); recurs dozens of times, flagged once as representative |
| Override-or-default coalescing | declarative_materialization.py:2468-2616 | ~15 leaf entries do `params.get(override) or plan.&lt;computed_default&gt;` | borderline | `ValueQuery`/`Coerce` don't model fallback to a computed non-payload default |
| Filesystem-`exists()` gating conflated with payload selection | declarative_materialization.py:2403-2461, 2619-2676 | Artifact inclusion gated on disk `path.exists()` | borderline | `Compare(op="exists")` means payload-path existence, not filesystem existence — same word, different concept; must not be conflated during migration |
| `_walk_manifest_strings` / `_manifest_npz_refs` | rollout_cleanup.py:281-297 | Recursive walk of arbitrary JSON yielding every string leaf ending `.npz`, key-name-independent | borderline | Schema-agnostic recursive suffix-match doesn't fit `ValueQuery`'s fixed-path model; needs a wildcard/recursive path type |
| `_candidate_npz_files` containment check | rollout_cleanup.py:258-278 | Filesystem glob + manifest refs; raises if a manifest-referenced `.npz` falls outside the bulk dir | borderline | Containment predicate could be declarative but is fused with filesystem enumeration |
| `_artifact_record` file-vs-dir branch | rollout_cleanup.py:300-313 | `is_file()`/`is_dir()` branch: single-file hash vs `rglob` aggregate (`sum`/`len`) | borderline | Dir branch does real aggregation — blocks any pure framing |
| Cleanup-manifest payload assembly | rollout_cleanup.py:129-144 | Final dict from computed aggregates (`len`, `sum(size_bytes)`, accumulated bytes) | borderline | Textbook aggregation-blocks-ValueQuery; completeness only |
| Report params defaulting | reports.py:60-154 | Pydantic defaulting of `source_artifact_roles`/`title` keyed by `report_type` against class-level lookup dicts | borderline | Declarative-shaped, but already routed through rlrmp's params-model/schema-migration contract — flagged for shape, not as unmigrated |
| `_resolve_repo_path` URI-scheme dispatch | training_diagnostics.py:345-356 | if/elif on `repo://`/`file://` prefixes + `(root/path).exists()` directory preference | borderline | Scheme resolution/path probing drives I/O branching, not data selection |
| `_finite_check_view` masking gate | training_diagnostics.py:383-395 | ANDed cascade (non-None + `startswith` + shape match) deciding boolean ndarray indexing | borderline | Structurally `AllOf(Compare)` but operates on live numpy shapes for numeric masking |
| `PerturbationSpec.to_json` conditional serialization | perturbation_rows.py:225-273 | ~10 `if self.field is not None:` conditionals + flattened provenance/extra | borderline | Encode direction (dataclass → JSON); "omit if None" doesn't map onto the decode-framed primitives |
| gamma-factor guard | math/adversary_equivalence.py:319-320 | Single equality check raising on mismatch | borderline | One-field runtime invariant on freshly materialized data; no payload/file to bind an Expr to |
| `_cost_dict` | math/adversary_equivalence.py:389-397 | Copies/renames 6 fields off in-memory `RolloutCost` into flat dict | borderline | Rename-only ValueQuery shape, but source is a computed in-process object |
| `result_summary` | math/adversary_equivalence.py:400-464 | Summary dict from dataclass fields plus derived arithmetic (differences, ratios, norms) | borderline | Genuine computation exceeds pure extraction even if file-sourced |
| Frontier row lookups | math/cs_game_card.py:469-473 | Three `next(row for row in frontier if row["factor"] == &lt;const&gt;)` single-match picks | borderline | Structurally `Select(where)` (single-match-or-raise) over an ephemeral in-memory payload |
| `_npz_arrays` per-gamma key construction | math/cs_game_card.py:621-645 | Loops gamma refs, builds dynamic keys, 9 renamed/`np.asarray`-coerced entries per gamma + 10 fixed | borderline | Rename+coerce matches `FieldMapping`/`Coerce`, but select-all exceeds `Select`'s single-match contract and source is live arrays |
| `reference_summary` payload assembly | math/cs_game_card.py:350-458 | JSON-serializable summary: renames + literals + real arithmetic (`delta_v = 100.0*(peak-lqr_peak)/lqr_peak` at 363) | borderline | Mixed extraction/computation; separating the pure subset would fragment one cohesive step |
| `_walk_model_for_plant_params` | math/hinf_riccati.py:1019-1063 | PyTree walk with `hasattr` probes + try/except-around-float-cast to discover `dt`/`tau` | borderline | Exactly the B shape, but the "payload" is an arbitrary object graph, not declared-schema JSON |
| `CostSpec.R_weight` allowlisted scalar | math/hinf_riccati.py:1179-1187 | Adopted-run-derived default `3.0e-5` with inline `ea6ccb4` rationale | borderline | Already adjudicated per repo policy; noted because the `loss_update.converged_R` fallback (~1454) reads the same kind of adopted value from config rather than a governed product |
| Select-by-label idiom | math/linear_equivalence_certificate.py:181-184, 406-407, 426-433 | `next(x for x in audits/metrics if x.label == ...)` | borderline | Exact `Select` semantics; blocked from A only because source is a live dataclass — needs the results to flow through manifests first |
| Select-by-`gamma_factor` idiom (×9) | math/robust_bellman.py:597, 703, 857, 1002, 1089, 1173, 1392, 1754, 1757 | `next(ref for ref in reference.gamma_references if ref.factor == gamma_factor)` verbatim across 7 sibling functions | borderline | Strongest future-`Select` target in the sweep by repetition, if `gamma_references` is ever persisted/re-loaded |
| Select-by-label feeding ratios | math/output_feedback.py:2129-2136, 2176-2189 | `next((row[...] for row in exact_rows if row["label"]==...), None)` inside division expressions | borderline | Selection is pure `Select` shape; fused ratio arithmetic blocks pure `ValueQuery` |
| Compound-predicate row selects (×3) | math/output_feedback.py:2530-2537, 2546-2551, 2553-2562 | `selected_row`/`chosen_row`/`next_safer` each pick one row from `summary["rows"]` by predicates | borderline | Richest structural match in math/ — pure single-row predicate selection over the very dict serialized to the tracked gamma-sweep manifest moments later (inside the skipped LEGACY writer); near-portable to `Select` if the manifest were re-loaded as a context item |
| `audits_by_prefix` rename+lookup | math/output_feedback.py:2389-2394 | `{row["label"].replace("_exact_kalman",""): row ...}` then keyed lookup | borderline | Join/rename with string-transformed keys — `ValueQuery`/`Coerce` don't do string transforms |
| Rename + join-by-`time_index` | math/robust_bellman.py:1263-1269 | Dict join across two lists + literal rename via `.pop` | borderline | Hand-rolled `FieldMapping` rename + Select-by-key join, purely structural |
| Argmax/argmin "select best" | math/linear_round_trip.py:479-482, 667-670; math/output_feedback.py:1866 | `max/min(..., key=...)` extremal picks | borderline | Pick-one-row spirit, but `Select` (predicate uniqueness) can't express argmin/argmax |
| Summary-dict digging | math/linear_equivalence_certificate.py:485-487, 494, 502 | Path-walks `summary["objective_trainings"][label][...]` into a dict the same function just built | borderline | ValueQuery-shaped walk into an ad-hoc in-memory manifest |
| `_classify_controller` + `overall_status` | math/linear_equivalence_certificate.py:369-389, 565-572 | Threshold if-cascades vs config tolerances → classification strings; `any(!=)` → overall status | borderline | `AllOf(Compare(le))`/`AnyOf(Not(Compare(eq)))` shaped, but produces new label values over non-manifest inputs |
| Enum validators + metadata builder | math/rerun_metadata.py:37-52, 55-76 | `value in CHOICES` membership gating `build_rerun_metadata`'s hand-assembled manifest fragment | borderline (weak) | Write-side builder/arg validation — likely out of scope for a read-side query language |
| `_npz_arrays` | math/output_feedback.py:2683-2737 | Flattens objects into labeled numpy dict for `np.savez_compressed` | borderline (weak) | Array-bundle construction outside `ValueQuery`'s scalar domain; completeness only |
| Recurring `params.get(...)` digging | matrix/standard_matrix.py:56-59, 77-79, 252-254, 272-275, 368, 455, 464-474 | `note_marker`/`notes_path`/`figure_routing`/`cell_metadata`/`profile_key`/`metric_order` digs through compute/emit/figure helpers | borderline | Densest recurrence of the less-migrated params-digging cousin of the YAML `run_condition` pattern outside declarative_materialization.py |

## Skipped (LEGACY) — analysis/ top-level + math/ + matrix/

**Whole-file skips: none.** No in-scope file carries a file-top LEGACY banner (verified by grep; the `pipelines/` LEGACY files belong to the sibling audit). Note: `frozen_adversary_audit.py`/`frozen_policy_gate.py` "frozen" naming refers to frozen-adversary/policy semantics, unrelated to the LEGACY convention.

**Function-scoped LEGACY skips** (banner text "Scoped legacy surface: `&lt;fn&gt;`. The math core in this module is LIVE..."; bodies not audited, surrounding files audited in full):

| file | banner line | function | body span skipped |
|---|---|---|---|
| math/adversary_equivalence.py | 591 | `write_outputs` | ~585-641 |
| math/cs_game_card.py | 654 | `write_outputs` | ~648-698 |
| math/linear_equivalence_certificate.py | 682 | `write_outputs` | ~676-743 |
| math/linear_round_trip.py | 940 | `write_outputs` | ~934-1006 |
| math/output_feedback.py | 1292 | `analyze_phase0b_output_feedback` | 1289-1331 |
| math/output_feedback.py | 1469 | `analyze_phase1_output_feedback` | 1461-1607 |
| math/output_feedback.py | 1900 | `analyze_phase3_output_feedback` | 1895-2048 — this skipped body contains a `results/`/`_artifacts/` `np.load` extraction site at ~1985-2026 that would otherwise be a strong Category A candidate; flagged for visibility, not counted per the skip rule |
| math/output_feedback.py | 2638 | `write_gamma_sweep_outputs` | 2631-2682 |
| math/output_feedback.py | 2746 | `write_outputs` | 2740-2822 (EOF) |
| math/robust_bellman.py | 2172 | `write_outputs` | ~2166-2228 |

## Files checked with no findings

- `analysis/__init__.py` — docstring + idempotent lazy recipe-registration trigger only
- `analysis/feedbax_controllers.py` — pure Feedbax graph/adapter construction; no JSON/manifest reads, no payload predicates
- `analysis/lambda_recommendations.py` — membership-set validation guards over caller-supplied in-memory dataclasses; no tracked I/O
- `analysis/math/__init__.py` — one-line docstring
- `analysis/math/cs_released_simulation.py` — pure JAX numerics (Kalman/extLQG recursions, rollouts); zero I/O, zero payload wrangling
- `analysis/math/induced_gain.py` — pure linear algebra/JAX (power iteration, Hamiltonian bisection); no `open()`, `json`, `Path`, or params access anywhere
- `analysis/math/trial_alignment.py` — pure numpy alignment/trim/reduce; no file I/O, no manifest/params handling
- `analysis/matrix/__init__.py` — re-exports only

**Cross-cutting synthesis for the sweep:** (1) The single strongest migration target is the feedback-quality component gating in `declarative_materialization.py` (1407-1470, 2619-2676), where each registration already names its intended condition as a `gating_spec` string but implements it procedurally. (2) `matrix/standard_matrix.py` is the best whole-file candidate — genuine near-A extraction over manifest-derived payloads plus clean fail-closed B gates. (3) Recurring query-language capability gaps that block otherwise-clean migrations: an "OR-default"/coalesce combinator (rollout_cleanup ×4, training_diagnostics ×2, declarative_materialization ×2, frozen_policy_gate ×1, standard_matrix multi-source coalescing), argmin/argmax selection, filter-to-many (vs `Select`'s exactly-one), string normalization/transforms before comparison, and residual-passthrough field mapping. (4) Filesystem `path.exists()` gating is repeatedly interleaved with payload selection and must not be conflated with `Compare(op="exists")` payload semantics. (5) The math/ core modules are largely query-language-clean: extraction-shaped code there operates on in-memory results that only the LEGACY writers serialize — routing those payloads through manifests is the prerequisite for porting the many `Select`-shaped `next(...)` idioms (9× `gamma_factor` in robust_bellman.py; 3 compound-predicate row picks in output_feedback.py's markdown renderer). (6) One concrete drift risk: `linear_round_trip.py` states its pass tolerances twice — in predicates (667-684) and as data in `predeclared_tolerances` (~734-739).
