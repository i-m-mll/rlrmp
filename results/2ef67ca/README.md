# Part 2.5 Legacy Training-Run Archive (`2ef67ca`)

This directory is the legacy Part 2.5 training-run archive. It holds pre-migration runs
(`models/`, `running_cost_nn1e6/`, the top-level `config.json`, and the
`centerout_*_pert1/` dirs) that retain their original `config.json` payloads — no
`run.json` migration was performed per the f485c26 final decisions (legacy archive policy).

For the Part 2.5 phase overview (research question, sub-phases, results tables, and
interpretation), see `results/844ef95/README.md` (umbrella issue `844ef95`).
