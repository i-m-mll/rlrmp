# Beta 1.4 Soft Run Lock

Issue `b413bb0` prepares the no-launch lock for the next beta 1.4 soft-adversary
comparison. The prepared rows are `direct_epsilon`, `linear_no_bias`, and
`affine`; no pod acquisition or training launch is authorized by these files.
