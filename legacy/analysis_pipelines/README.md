# Frozen analysis pipelines

This directory holds frozen, pre-contract analysis pipeline modules retained for
provenance while their report-stage replacements remain open. They are outside
`src/` so they cannot be imported as live `rlrmp` runtime modules.

See `results/3cf909c/notes/legacy_materializers.md` for the retention inventory
and issue `ef8e1df` for the compatibility-retirement work that relocated them.
